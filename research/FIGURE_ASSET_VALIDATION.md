# FIGURE_ASSET_VALIDATION.md — Figure Asset Check

> **Date**: 2026-09-08

---

## Status

All 4 figures are **vector PDF, fonts embedded**, generated by a single
Python script. No manual/Illustrator-only figures. PNGs exist as previews
only.

| Figure | File | Format | Fonts embedded | Vector | Source |
|--------|------|--------|----------------|--------|--------|
| 1 System Overview | `system_overview.pdf` | PDF | ✅ (DejaVuSerif) | ✅ | `gen_all_figs.py` fig1 |
| 2 Budget Mismatch + CM | `budget_mismatch.pdf` | PDF | ✅ (DejaVu) | ✅ | `gen_all_figs.py` fig2 |
| 3 Regime Effect | `regime_effect.pdf` | PDF | ✅ (DejaVu) | ✅ | `gen_all_figs.py` fig3 |
| 4 Three-Arm Ablation | `three_arm_ablation.pdf` | PDF | ✅ (DejaVu) | ✅ | `gen_all_figs.py` fig4 |

## IEEE requirements checked

| Requirement | Status |
|-------------|--------|
| Vector format accepted (PDF/EPS/TIFF) | ✅ PDF accepted |
| Fonts embedded | ✅ verified via `pdffonts` (all `emb: yes`) |
| Line width legible at double-column width | ✅ generated at single-column width (`figsize=(3.4, 5.2)` etc.) |
| Font size legible (≥ ~6pt) | ✅ 7.5pt serif default in script |
| Grayscale readability | ✅ black/white boxes + grayscale fills (#f0f0f0); no color-coded content |
| Caption consistency with paper | ✅ captions verified in `results.tex`/`algebra.tex` |

## PNG policy

- PNGs (`system_overview.png`, `budget_mismatch.png`, `regime_effect.png`,
  `three_arm_ablation.png`) are **preview-only**.
- The paper `\includegraphics` references **PDF** (vector), so the formal
  submission source is vector. PNGs may be deleted or kept as preview.
- `coverage_main.png` is a **legacy artifact** from the superseded chain-law
  draft — not referenced by current paper. Should be removed or kept in
  archive only.