benchmark_root: benchmark
output_root: runs
datasets:
  - hotpotqa
  - 2wikimultihop
  - musique
  - strategyqa
  - drop
seed: 161803
random_seeds: [161803, 161804, 161805, 161806, 161807]
budget:
  max_steps: 10
  max_llm_calls: 64
  max_retrieval_calls: 12
  question_timeout_seconds: 900
stages:
  answer_contract_train_v1:
    split: train
    sample_size: 20
    methods:
      - slotrag
      - slotrag-grounded-frontier-guard
      - slotrag-grounded-frontier-answer-contract
    frozen_plan_source: slotrag
