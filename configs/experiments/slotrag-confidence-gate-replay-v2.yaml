benchmark_root: benchmark
output_root: runs
datasets:
  - hotpotqa
  - 2wikimultihop
  - musique
  - strategyqa
  - drop
seed: 13141
random_seeds: [13141, 13142, 13143, 13144, 13145]
budget:
  max_steps: 10
  max_llm_calls: 64
  max_retrieval_calls: 12
  question_timeout_seconds: 300
stages:
  confidence_gate_replay:
    split: evaluation
    sample_size: 100
    methods:
      - slotrag
      - slotrag-confidence-gated-dual-query-0p5
    frozen_plan_source: slotrag
    frozen_plan_import_dir: runs/slotrag-confidence-gate-replay-v1/plans/confidence_gate_replay
