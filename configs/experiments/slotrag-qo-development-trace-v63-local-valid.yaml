benchmark_root: benchmark
output_root: runs
datasets:
  - hotpotqa
  - 2wikimultihop
seed: 271828
random_seeds: [271828]
budget:
  max_steps: 8
  max_llm_calls: 48
  max_retrieval_calls: 8
  question_timeout_seconds: 600
stages:
  qo_trace_local_dev_v63_valid:
    split: train
    sample_size: 40
    methods: [slotrag]
    retrieval_protocol: local_context
    retrieval_backend: bm25
