benchmark_root: benchmark
output_root: runs
datasets:
  - hotpotqa
  - 2wikimultihop
seed: 314159
random_seeds: [314159]
budget:
  max_steps: 8
  max_llm_calls: 48
  max_retrieval_calls: 8
  question_timeout_seconds: 600
stages:
  qo_trace_global_dev_v66_validation:
    split: train
    sample_size: 40
    methods: [slotrag]
    retrieval_protocol: global_corpus
    retrieval_backend: bm25
    shared_index_dir: runs/slotrag-global-index-v64/runner-registry
