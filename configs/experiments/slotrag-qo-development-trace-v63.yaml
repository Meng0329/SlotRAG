benchmark_root: benchmark
output_root: runs
datasets:
  - hotpotqa
  - 2wikimultihop
seed: 271828
random_seeds: [271828]
budget:
  max_steps: 8
  max_llm_calls: 48
  max_retrieval_calls: 8
  question_timeout_seconds: 600
stages:
  qo_trace_local_smoke_v63:
    split: train
    sample_size: 2
    methods: [slotrag]
    retrieval_protocol: local_context
    retrieval_backend: bm25
  qo_trace_local_dev_v63:
    split: train
    sample_size: 40
    methods: [slotrag]
    retrieval_protocol: local_context
    retrieval_backend: bm25
  qo_trace_global_dev_v63:
    split: train
    sample_size: 40
    methods: [slotrag]
    retrieval_protocol: global_corpus
    retrieval_backend: bm25
    shared_index_dir: runs/slotrag-qo-development-trace-v63/corpus

