benchmark_root: benchmark
output_root: runs
datasets:
  - hotpotqa
  - 2wikimultihop
  - musique
  - strategyqa
  - drop
seed: 141421
random_seeds: [141421, 141422, 141423, 141424, 141425]
budget:
  max_steps: 10
  max_llm_calls: 64
  max_retrieval_calls: 12
  question_timeout_seconds: 900
stages:
  adaptive_answer_contract_train_v1:
    split: train
    sample_size: 20
    methods:
      - slotrag
      - slotrag-grounded-frontier-guard
      - slotrag-grounded-frontier-answer-contract
    frozen_plan_source: slotrag
