benchmark_root: benchmark
output_root: runs
datasets:
  - hotpotqa
  - 2wikimultihop
  - musique
  - strategyqa
  - drop
seed: 314159
random_seeds: [314159, 314160, 314161, 314162, 314163]
budget:
  max_steps: 10
  max_llm_calls: 64
  max_retrieval_calls: 12
  question_timeout_seconds: 900
stages:
  frontier_guard_train_v2:
    split: train
    sample_size: 100
    methods:
      - slotrag
      - slotrag-grounded-binding-guard
      - slotrag-grounded-frontier-guard
    frozen_plan_source: slotrag
